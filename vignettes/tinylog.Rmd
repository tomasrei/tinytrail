---
title: "Getting started with tinylog"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Getting started with tinylog}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", eval = FALSE)
```

`tinylog` keeps a YAML registry of which scripts produced which output files
in your project. Place three lightweight calls in each script and the registry
stays current automatically.

## The three functions

| Function | Where to use it |
|---|---|
| `tinylog()` | Top of every script |
| `tinylog_write()` | Inline around any file path you save to |
| `tinylog_dict()` | After reading/cleaning a data frame |

Short aliases (`tl_write()`, `tl_dict()`) are available for interactive use.

## A minimal workflow

```{r workflow}
library(tinylog)

# --- top of 01_clean.R ---
tinylog(
  data_source = "The Current Population Survey (BLS)",
  description = "Clean and reshape survey data"
)

# read and clean
survey <- read.csv("data/raw/survey.csv") |>
  tinylog_dict()                           # records column names + samples

# save — wrap the file path with tinylog_write()
write.csv(
  survey,
  file = tinylog_write("data/clean/survey_clean.csv")
)
```

`tinylog_write()` is a thin wrapper that logs the path and returns it unchanged,
so it slots directly into the `file =` argument of any write function. It works
equally well with `here::here()`:

```{r here}
write.csv(
  survey,
  file = tinylog_write(here::here("data/clean/survey_clean.csv"))
)
```

That is all you need to add. The registry file (`_tinylog.yaml`) is
created in the project root on the first run and updated on every subsequent
run.

## The registry file

After running a few scripts, `_tinylog.yaml` looks like this:

```yaml
$version: 0.1.0
$learn_more: https://github.com/tomasrei/tinylog
scripts:
  01_clean.R:
    data_source: data/raw/survey.csv
    description: Clean and reshape survey data
    first_run: '2026-01-15 09:12'
    latest_run: '2026-01-20 11:05'
    script_runtime: 0.3 min
    n_files: 1
    outputs:
    - data/clean/survey_clean.csv
  02_model.R:
    data_source: data/clean/survey_clean.csv
    description: Fit logistic regression
    first_run: '2026-01-16 14:22'
    latest_run: '2026-01-20 11:08'
    script_runtime: 1.4 min
    n_files: 2
    outputs:
    - results/model_fit.rds
    - results/table_coefficients.csv
data_dictionary:
  01_clean.R:
    survey:
      columns:
        id: [1, 2, 3, 4, 5]
        age: [34, 52, 28, 41, 37]
        response: ['yes', 'no', 'yes', 'yes', 'no']
```

## Options

Pin an important script to the top of the registry:

```{r pin}
tinylog(
  data_source = "data/raw/master.csv",
  description = "Master data preparation",
  pin_to_top  = TRUE
)
```
