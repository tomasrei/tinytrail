% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinytrail.R
\name{tinytrail_write}
\alias{tinytrail_write}
\title{Record an output file path in the trail}
\usage{
tinytrail_write(file)
}
\arguments{
\item{file}{Character. Path to the output file.}
}
\value{
\code{file}, invisibly.
}
\description{
Wraps the file path argument of any save call. Registers the path under the
current script's trail entry and returns the path unchanged, so it can be
dropped inline into any save function.
}
\details{
Requires \code{\link[=tinytrail]{tinytrail()}} to have been called first in the same session.
}
\examples{
\donttest{
tmp <- tempfile()
dir.create(tmp)
writeLines("Version: 1.0", file.path(tmp, "DESCRIPTION"))
old_wd <- setwd(tmp)
tinytrail("raw/data.csv", "example script", name = "script.R", record_runtime = FALSE)
out <- tinytrail_write(file.path(tmp, "output.csv"))
setwd(old_wd)
unlink(tmp, recursive = TRUE)
}
}
