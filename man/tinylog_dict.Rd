% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{tinylog_dict}
\alias{tinylog_dict}
\alias{tl_dict}
\title{Add a data frame to the project data dictionary}
\usage{
tinylog_dict(df, name = NULL, sample_values = TRUE, sample_string_length = 18L)

tl_dict(df, name = NULL, sample_values = TRUE, sample_string_length = 18L)
}
\arguments{
\item{df}{A data frame.}

\item{name}{Character. Label for this entry. Defaults to \code{"input_1"}, \code{"input_2"}, etc.}

\item{sample_values}{Logical. Record 5 sample values per column. Default \code{TRUE}.}

\item{sample_string_length}{Integer or \code{Inf}. Maximum characters per sample value before truncating with \code{"..."}. Default \code{18L}.}
}
\value{
\code{df}, invisibly.
}
\description{
Place at the end of a read/clean pipeline to capture column names and
optionally sample values. Returns the data frame unchanged.
}
\details{
\code{tl_dict()} is a short alias for \code{tinylog_dict()}.

Requires \code{\link[=tinylog_script]{tinylog_script()}} to have been called first in the same session.
}
\examples{
\dontrun{
dat <- read.csv(here::here("data/raw/file.csv")) |> tinylog_dict()
dat <- readRDS(here::here("data/clean/file.rds")) |> tinylog_dict("occupation")
}
}
