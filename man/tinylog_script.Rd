% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinylog.R
\name{tinylog_script}
\alias{tinylog_script}
\alias{tl_script}
\title{Register a script in the project registry}
\usage{
tinylog_script(
  data_source,
  description,
  name = .get_current_script_name(),
  pin_to_top = FALSE,
  record_runtime = TRUE,
  yaml_file = getOption("tinylog.file", "_tinylog_proj.yaml")
)

tl_script(
  data_source,
  description,
  name = .get_current_script_name(),
  pin_to_top = FALSE,
  record_runtime = TRUE,
  yaml_file = getOption("tinylog.file", "_tinylog_proj.yaml")
)
}
\arguments{
\item{data_source}{Character. The input data this script reads (path or description).}

\item{description}{Character. One-line description of what the script does.}

\item{name}{Character. Script name. Detected automatically when run via \code{source()}.}

\item{pin_to_top}{Logical. Pin this script to the top of the registry. Default \code{FALSE}.}

\item{record_runtime}{Logical. Record elapsed time on exit. Default \code{TRUE}.}

\item{yaml_file}{Character. Registry file name. Defaults to the \code{tinylog.file} option,
or \code{"_tinylog_proj.yaml"} if unset. Set \code{options(tinylog.file = "my_log.yaml")} to
change it project-wide.}
}
\value{
\code{name} (the script name), invisibly. Called for its side effect of
creating or updating the YAML registry file in the project root.
}
\description{
Call once near the top of every script, after sourcing this package.
Creates or updates an entry in \verb{_registry.yaml} and sets the script name
so that \code{\link[=tinylog_output]{tinylog_output()}} can associate outputs with it.
}
\details{
\code{tl_script()} is a short alias for \code{tinylog_script()}.
}
\examples{
\donttest{
tmp <- tempfile()
dir.create(tmp)
writeLines("Version: 1.0", file.path(tmp, "DESCRIPTION"))
old_wd <- setwd(tmp)
tinylog_script(
  data_source    = "data/raw/survey.csv",
  description    = "Clean and reshape survey data",
  name           = "01_clean.R",
  record_runtime = FALSE
)
setwd(old_wd)
unlink(tmp, recursive = TRUE)
}
}
